import { analyzeMountain } from './core/libra-mountain.mjs';

const $=id=>document.getElementById(id);
const money=v=>`${Number(v||0)>=0?'+':'-'}$${Math.abs(Number(v||0)).toFixed(2)}`;
const clamp=(v,min,max)=>Math.max(min,Math.min(max,Number(v)||0));
const KEY='sani.comet.paper.v1';
let ws=null,ticks=[],page=0,auto=false,position=null;
let session=loadSession();
let latestMountain=analyzeMountain([]);

function freshSession(){return{pnl:0,trades:[],tape:[],peak:0,trough:0,maxDrawdown:0,equity:0}}
function loadSession(){try{return{...freshSession(),...(JSON.parse(localStorage.getItem(KEY)||'null')||{})}}catch{return freshSession()}}
function save(){try{localStorage.setItem(KEY,JSON.stringify(session))}catch{}}
function addTape(type,text,extra={}){session.tape.unshift({at:Date.now(),type,text,...extra});session.tape=session.tape.slice(0,800);save();renderTape()}
function tickStep(rows=ticks){const d=[];for(let i=Math.max(1,rows.length-80);i<rows.length;i++)d.push(Math.abs(rows[i].quote-rows[i-1].quote));d.sort((a,b)=>a-b);return d.length?d[Math.floor(d.length/2)]||1:1}
function regimeLabel(m){if(m.direction==='CHOP'||m.direction==='NONE')return m.direction;return `${m.direction} · ${m.entryMode}`}
function allowedSide(m){return m.allowedDirection==='CALL'?'LONG':m.allowedDirection==='PUT'?'SHORT':'NONE'}

function candidate(m){
  if(!auto||position||!m?.ready)return null;
  if(!['UP','DOWN'].includes(m.direction))return null;
  if(!['WAIT_PULLBACK_END','PULLBACK_END','EARLY_MOMENTUM'].includes(m.entryMode))return null;
  const quote=ticks.at(-1)?.quote;if(!Number.isFinite(quote))return null;
  const riskDollars=Math.max(.1,Number($('riskDollars')?.value||1));
  const targetR=clamp($('targetR')?.value||2,1,8);
  const step=tickStep();
  const side=m.direction==='UP'?'LONG':'SHORT';
  const structureStop=Number(m.important?.quote);
  let stop=Number.isFinite(structureStop)?structureStop:side==='LONG'?quote-step*4:quote+step*4;
  if(side==='LONG'&&stop>=quote)stop=quote-step*3;
  if(side==='SHORT'&&stop<=quote)stop=quote+step*3;
  const riskDistance=Math.abs(quote-stop);if(!(riskDistance>0))return null;
  const target=side==='LONG'?quote+riskDistance*targetR:quote-riskDistance*targetR;
  const units=riskDollars/riskDistance;
  return{side,entry:quote,stop,target,units,riskDollars,targetR,openedAt:Date.now(),openedEpoch:ticks.at(-1)?.epoch,mountain:structuredClone(m),bestR:0,trailStop:stop};
}
function openPosition(c){position=c;addTape('ENTRY',`${c.side} opened @ ${c.entry.toFixed(2)} · stop ${c.stop.toFixed(2)} · target ${c.target.toFixed(2)}`,{side:c.side});renderAll()}
function unrealized(p=position,quote=ticks.at(-1)?.quote){if(!p||!Number.isFinite(Number(quote)))return{pnl:0,r:0};const delta=p.side==='LONG'?quote-p.entry:p.entry-quote;const pnl=delta*p.units;return{pnl,r:p.riskDollars?pnl/p.riskDollars:0}}
function closePosition(reason,quote=ticks.at(-1)?.quote){if(!position||!Number.isFinite(Number(quote)))return;const u=unrealized(position,quote);const trade={...position,exit:Number(quote),closedAt:Date.now(),pnl:Number(u.pnl.toFixed(4)),r:Number(u.r.toFixed(4)),reason};session.trades.unshift(trade);session.pnl=Number((session.pnl+trade.pnl).toFixed(4));session.equity=session.pnl;session.peak=Math.max(session.peak,session.equity);session.trough=Math.min(session.trough,session.equity);session.maxDrawdown=Math.max(session.maxDrawdown,session.peak-session.equity);addTape('EXIT',`${trade.side} closed ${reason} · ${money(trade.pnl)} · ${trade.r.toFixed(2)}R`,{side:trade.side});position=null;save();renderAll()}
function managePosition(m){if(!position)return;const q=ticks.at(-1)?.quote;if(!Number.isFinite(q))return;const u=unrealized(position,q);position.bestR=Math.max(position.bestR||0,u.r);
  const thesisBroken=(position.side==='LONG'&&m.direction==='DOWN')||(position.side==='SHORT'&&m.direction==='UP')||m.direction==='CHOP';
  if(position.side==='LONG'&&q<=position.trailStop)return closePosition('STOP / INVALIDATION',q);
  if(position.side==='SHORT'&&q>=position.trailStop)return closePosition('STOP / INVALIDATION',q);
  if(position.side==='LONG'&&q>=position.target)return closePosition('TARGET',q);
  if(position.side==='SHORT'&&q<=position.target)return closePosition('TARGET',q);
  if(thesisBroken)return closePosition('MOUNTAIN REVERSAL',q);
  // Earn the trail: after +1R, protect half an R. After +1.5R, trail behind fresh important structure if it improves risk.
  if(u.r>=1){const be=position.side==='LONG'?position.entry+Math.abs(position.entry-position.stop)*.15:position.entry-Math.abs(position.entry-position.stop)*.15;position.trailStop=position.side==='LONG'?Math.max(position.trailStop,be):Math.min(position.trailStop,be)}
  if(u.r>=1.5&&Number.isFinite(Number(m.important?.quote))){const s=Number(m.important.quote);if(position.side==='LONG'&&s>position.trailStop&&s<q)position.trailStop=s;if(position.side==='SHORT'&&s<position.trailStop&&s>q)position.trailStop=s}
}
function onTick(epoch,quote){if(!Number.isFinite(epoch)||!Number.isFinite(quote))return;const last=ticks.at(-1);if(last&&last.epoch===epoch&&last.quote===quote)return;ticks.push({epoch,quote});if(ticks.length>4000)ticks.splice(0,ticks.length-4000);latestMountain=analyzeMountain(ticks);managePosition(latestMountain);if(!position){const c=candidate(latestMountain);if(c)openPosition(c)}renderAll()}

function connect(){const appId=$('cometAppId')?.value?.trim();if(!appId){$('cometFeedStatus').textContent='APP ID NEEDED';return}disconnect();$('cometFeedStatus').textContent='CONNECTING';try{ws=new WebSocket(`wss://ws.derivws.com/websockets/v3?app_id=${encodeURIComponent(appId)}`);ws.onopen=()=>{ws.send(JSON.stringify({ticks:$('cometSymbol').value.trim()||'1HZ25V',subscribe:1}));$('cometFeedStatus').textContent='LIVE';addTape('SYSTEM','Public market feed connected.')};ws.onmessage=e=>{let d;try{d=JSON.parse(e.data)}catch{return}if(d.error){$('cometFeedStatus').textContent=d.error.code||'ERROR';return}if(d.tick)onTick(Number(d.tick.epoch),Number(d.tick.quote))};ws.onclose=()=>{$('cometFeedStatus').textContent='OFFLINE'};ws.onerror=()=>{$('cometFeedStatus').textContent='ERROR'}}catch{$('cometFeedStatus').textContent='ERROR'}}
function disconnect(){if(ws){try{ws.close()}catch{}ws=null}$('cometFeedStatus')&&($('cometFeedStatus').textContent='OFFLINE')}
async function loadAccounts(){const appId=$('cometAppId').value.trim(),token=$('cometToken').value.trim();if(!appId||!token){$('cometAccountStatus').textContent='APP ID + TOKEN';return}const btn=$('cometLoadAccounts');btn.disabled=true;try{const r=await fetch('/api/accounts',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({appId,token})}),d=await r.json();if(!r.ok)throw new Error(d.error||'Account load failed');const demos=(d.accounts||[]).filter(a=>String(a.account_type||'').toLowerCase()!=='real'),sel=$('cometAccount');sel.innerHTML=demos.length?'':'<option value="">No Demo accounts</option>';for(const a of demos){const o=document.createElement('option');o.value=a.account_id;o.textContent=`DEMO · ${a.account_id} · ${a.currency} ${a.balance}`;sel.appendChild(o)}$('cometAccountStatus').textContent=demos.length?`${demos.length} DEMO`:'NONE';sessionStorage.setItem('sani.comet.token',token);localStorage.setItem('sani.comet.appId',appId)}catch(e){$('cometAccountStatus').textContent='ERROR';addTape('ERROR',e.message)}finally{btn.disabled=false}}

function switchPage(n){page=clamp(n,0,5);document.querySelectorAll('[data-comet-page]').forEach((el,i)=>{const on=i===page;el.classList.toggle('active',on);el.setAttribute('aria-hidden',String(!on))});document.querySelectorAll('[data-comet-target]').forEach(btn=>btn.classList.toggle('active',Number(btn.dataset.cometTarget)===page));if(page===1)drawChart()}
function renderAll(){const q=ticks.at(-1)?.quote;$('cometTickCount').textContent=ticks.length.toLocaleString();$('cometLastQuote').textContent=Number.isFinite(q)?`${$('cometSymbol').value} · ${q.toFixed(2)}`:'waiting for price';$('mapMountain').textContent=latestMountain.direction||'WARMING';$('mapPermission').textContent=`${allowedSide(latestMountain)} ONLY`; $('mapMoment').textContent=latestMountain.entryMode||'—';$('mapReason').textContent=latestMountain.reason||'waiting for shape';
  const u=unrealized();$('mapHand').textContent=position?position.side:auto?'SCANNING':'STAND DOWN';$('mapThesis').textContent=position?`${position.side} thesis alive · trail ${position.trailStop.toFixed(2)}`:auto?'Waiting for a Libra-quality directional window.':'Paper auto is paused.';$('mapR').textContent=`${u.r.toFixed(2)}R`;$('mapRisk').textContent=position?`risk ${money(position.riskDollars)} · target ${position.targetR.toFixed(2)}R`:'risk not armed';
  $('positionHeadline').textContent=position?`${position.side} the mountain.`:'Waiting for a worthy move.';$('positionSub').textContent=position?'Noise can wiggle. The thesis exits only at structural invalidation, target, or earned trail.':'COMET can watch forever. It does not need to manufacture a trade.';$('positionSide').textContent=position?position.side:'STAND DOWN';$('positionEntry').textContent=position?position.entry.toFixed(2):'—';$('positionStop').textContent=position?position.trailStop.toFixed(2):'—';$('positionTarget').textContent=position?position.target.toFixed(2):'—';$('positionPnl').textContent=money(u.pnl);$('positionR').textContent=`${u.r.toFixed(2)}R`;
  $('cometPnl').textContent=money(session.pnl+u.pnl);$('cometState').textContent=auto?'PAPER ACTIVE':'PAPER LAB';const goal=Math.max(1,Number($('goalDollars')?.value||650));$('goalFill').style.width=`${clamp(session.pnl/goal*100,0,100)}%`;$('goalCaption').textContent=`${money(session.pnl)} / $${goal.toFixed(0)}`;$('footerGoal').textContent=`$${goal.toFixed(0)}`;renderResults();renderTape();if(page===1)drawChart()}
function renderResults(){const trades=session.trades||[],wins=trades.filter(t=>t.pnl>0).length;$('statPnl').textContent=money(session.pnl);$('statTrades').textContent=trades.length;$('statWinRate').textContent=`${trades.length?(wins/trades.length*100).toFixed(1):'0.0'}%`;$('statBest').textContent=money(trades.length?Math.max(...trades.map(t=>t.pnl)):0);$('statWorst').textContent=money(trades.length?Math.min(...trades.map(t=>t.pnl)):0);$('statDrawdown').textContent=money(-Math.abs(session.maxDrawdown||0));const body=$('resultsBody');if(!trades.length){body.innerHTML='<tr><td colspan="7">No positions closed yet.</td></tr>';return}body.innerHTML=trades.slice(0,100).map(t=>`<tr><td>${new Date(t.closedAt).toLocaleTimeString()}</td><td>${t.side}</td><td>${t.entry.toFixed(2)}</td><td>${t.exit.toFixed(2)}</td><td>${t.reason}</td><td>${t.r.toFixed(2)}R</td><td>${money(t.pnl)}</td></tr>`).join('')}
function renderTape(){const list=$('tapeList'),rows=session.tape||[];$('tapeCount').textContent=`${rows.length} EVENTS`;list.innerHTML=rows.length?rows.slice(0,200).map(r=>`<div class="tapeEvent"><span>${new Date(r.at).toLocaleTimeString()}</span><b>${r.type}</b><span>${r.text}</span><em>${r.side||''}</em></div>`).join(''):'<p>Nothing yet.</p>'}
function drawChart(){const c=$('cometChart');if(!c)return;const dpr=window.devicePixelRatio||1,w=Math.max(600,c.clientWidth),h=Math.max(380,c.clientHeight);if(c.width!==Math.round(w*dpr)||c.height!==Math.round(h*dpr)){c.width=Math.round(w*dpr);c.height=Math.round(h*dpr)}const ctx=c.getContext('2d');ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,w,h);ctx.fillStyle='#080709';ctx.fillRect(0,0,w,h);const rows=ticks.slice(-260);if(rows.length<2){ctx.fillStyle='#b98ca0';ctx.font='12px IBM Plex Mono';ctx.fillText('CONNECT LIVE MARKET TO DRAW COMET',28,40);return}const pad={l:42,r:84,t:30,b:36},qs=rows.map(r=>r.quote),lo=Math.min(...qs),hi=Math.max(...qs),range=hi-lo||1,x=i=>pad.l+i/(rows.length-1)*(w-pad.l-pad.r),y=q=>pad.t+(hi-q)/range*(h-pad.t-pad.b);ctx.strokeStyle='rgba(255,183,216,.10)';ctx.lineWidth=1;for(let i=0;i<6;i++){const yy=pad.t+i/5*(h-pad.t-pad.b);ctx.beginPath();ctx.moveTo(pad.l,yy);ctx.lineTo(w-pad.r,yy);ctx.stroke()}ctx.strokeStyle='#8df5df';ctx.lineWidth=1.8;ctx.beginPath();rows.forEach((r,i)=>i?ctx.lineTo(x(i),y(r.quote)):ctx.moveTo(x(i),y(r.quote)));ctx.stroke();
  // Structure path from Libra pivots.
  const m=analyzeMountain(rows);const piv=(m.pivots||[]).filter(p=>p.index>=Math.max(0,(m.pivots||[])[0]?.index||0));if(piv.length>1){ctx.strokeStyle='#ff8fc3';ctx.lineWidth=1.3;ctx.setLineDash([7,6]);ctx.beginPath();piv.forEach((p,i)=>{const idx=rows.findIndex(r=>r.epoch===p.epoch&&r.quote===p.quote);if(idx<0)return;i?ctx.lineTo(x(idx),y(p.quote)):ctx.moveTo(x(idx),y(p.quote))});ctx.stroke();ctx.setLineDash([])}
  function line(q,color,label){if(!Number.isFinite(Number(q)))return;ctx.strokeStyle=color;ctx.lineWidth=1;ctx.setLineDash([5,4]);ctx.beginPath();ctx.moveTo(pad.l,y(q));ctx.lineTo(w-pad.r,y(q));ctx.stroke();ctx.setLineDash([]);ctx.fillStyle=color;ctx.font='10px IBM Plex Mono';ctx.fillText(label,w-pad.r+8,y(q)+3)}
  if(position){line(position.trailStop,'#ff6677','STOP');line(position.target,'#67e7a0','TARGET');line(position.entry,'#ffacd0','ENTRY')}
  for(const t of (session.trades||[]).slice(0,20)){const idx=rows.findIndex(r=>r.epoch===t.openedEpoch);if(idx<0)continue;ctx.fillStyle=t.side==='LONG'?'#79f0b4':'#ff687a';ctx.beginPath();if(t.side==='LONG'){ctx.moveTo(x(idx),y(t.entry)-8);ctx.lineTo(x(idx)-6,y(t.entry)+5);ctx.lineTo(x(idx)+6,y(t.entry)+5)}else{ctx.moveTo(x(idx),y(t.entry)+8);ctx.lineTo(x(idx)-6,y(t.entry)-5);ctx.lineTo(x(idx)+6,y(t.entry)-5)}ctx.closePath();ctx.fill()}
  ctx.fillStyle='#ffb3d2';ctx.font='11px IBM Plex Mono';ctx.fillText(`LIBRA MOUNTAIN: ${m.direction} · ${m.entryMode} · ${allowedSide(m)}`,pad.l,18)}

function bind(){document.querySelectorAll('[data-comet-target]').forEach(b=>b.addEventListener('click',()=>switchPage(Number(b.dataset.cometTarget))));document.querySelectorAll('[data-next]').forEach(b=>b.addEventListener('click',()=>switchPage(page+1)));document.querySelectorAll('[data-prev]').forEach(b=>b.addEventListener('click',()=>switchPage(page-1)));$('cometConnect').addEventListener('click',connect);$('cometDisconnect').addEventListener('click',disconnect);$('cometLoadAccounts').addEventListener('click',loadAccounts);$('startAuto').addEventListener('click',()=>{auto=true;addTape('SYSTEM','AUTO PAPER armed. COMET will wait for a directional setup.');renderAll()});$('pauseAuto').addEventListener('click',()=>{auto=false;addTape('SYSTEM','AUTO PAPER paused.');renderAll()});$('closePosition').addEventListener('click',()=>closePosition('MANUAL CLOSE'));$('resetComet').addEventListener('click',()=>{if(position)closePosition('RESET CLOSE');session=freshSession();position=null;auto=false;save();renderAll()});['riskDollars','targetR','goalDollars'].forEach(id=>$(id).addEventListener('input',renderAll));window.addEventListener('resize',()=>page===1&&drawChart())}
function boot(){const savedId=localStorage.getItem('sani.comet.appId');if(savedId)$('cometAppId').value=savedId;const tok=sessionStorage.getItem('sani.comet.token');if(tok)$('cometToken').value=tok;bind();switchPage(0);renderAll()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
